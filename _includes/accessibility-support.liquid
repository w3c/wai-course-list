{% for support in site.data.accessibility-support %}

<p class="fieldset-subtitles">{{ support.type }}</p>

{% for option in support.options %}

{% if option.options %}
<p class="expl">{{ option.name }}, {{strings.asupport_including}}: </p>
{% for details in option.options %}
<div class="radio-field">  
    <input type="checkbox" id="{{ details.id }}" name="asupport[]" value="{{ details.id }}" group="asupport">
    <label for="{{ details.id }}">{{ details.name }}</label>
</div>
{% endfor %}
{% else %}
<div class="radio-field">
    <input type="checkbox" id="{{ option.id }}" name="asupport[]" value="{{ option.id }}" group="asupport">
    <label for="{{ option.id }}">{{ option.name }}</label>
</div>    
{% endif %}
{% endfor %}
{% endfor %}

<div class="field">
    <label for="asupport_other">{{strings.asupport_other}}</label>
    <textarea id="asupport_other" name="asupport_other"></textarea>
</div>